name: PortierCI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

env:
  NODE_VERSION: "22"
  GO_VERSION: "1.25"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Lint
        run: npm run lint

  scripts:
    name: Scripts
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Version consistency
        run: npm run version:check
      - name: Script checks
        run: npm run validate:scripts

  postman:
    name: Postman
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Collection checks
        run: npm run validate:postman

  shared:
    name: Shared
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:shared
      - name: Typecheck
        run: npm run typecheck -w shared
      - name: Unit tests
        run: npm run test:shared
      - name: Coverage
        run: npm run validate:coverage:shared

  server:
    name: Server
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:shared && npm run build:server
      - name: Typecheck
        run: npm run typecheck -w server
      - name: Unit tests
        run: npm run test:server
      - name: Coverage
        run: npm run validate:coverage:server

  service:
    name: Service
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: service/go.sum
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:shared && npm run build:service
      - name: Generate OpenAPI
        run: npm run apidoc:generate
      - name: Unit tests
        run: npm run test:service
      - name: Coverage
        run: npm run validate:coverage:service
      - name: API parity
        run: npm run validate:openapi:go

  client:
    name: Client
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:shared && npm run build:server && npm run build:client
      - name: Typecheck
        run: npm run typecheck -w client
      - name: Unit tests
        run: npm run test:client
      - name: Coverage
        run: npm run validate:coverage:client
      - name: Install browsers
        run: npx playwright install --with-deps chromium
      - name: E2E tests
        run: npm run test:e2e

  cli:
    name: CLI
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: service/go.sum
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:cli
      - name: Unit tests
        run: npm run test:cli
      - name: Coverage
        run: npm run validate:coverage:cli

  replay:
    name: Replay
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: service/go.sum
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build:replay
      - name: Unit tests
        run: npm run test:replay
      - name: Coverage
        run: npm run validate:coverage:replay

  audit:
    name: Audit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: service/go.sum
      - name: Install
        run: npm ci
      - name: npm advisories
        run: npm audit --audit-level=high
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: Go advisories
        run: |
          govulncheck -C service ./...
          govulncheck -C tools/cli ./...
          govulncheck -C tools/replay ./...

  sanity:
    name: Sanity
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Version consistency
        run: npm run version:check
      - name: Script checks
        run: npm run validate:scripts
